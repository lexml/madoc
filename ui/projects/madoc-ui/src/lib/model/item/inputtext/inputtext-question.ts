import { TextQuestion } from '../shared//text-question';

export class InputTextQuestion extends TextQuestion {

  constructor() {
    super();
  }

  build(input: any) {
    super.build(input);
  }
}
